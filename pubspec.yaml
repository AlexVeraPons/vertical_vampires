name: vertical_vampires
description: vampire survivors like game but vertical.
publish_to: 'none'  

ublish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.1"

dependencies:
  bloc: ^9.0.0
  flame: ^1.25.0
  flame_forge2d: ^0.18.2+6
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0

dev_dependencies:
  flame_lint: ^1.2.2
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
